# Core
pandas>=2.2,<3.0
numpy>=1.26
scipy>=1.11              # Korrelationen (Spearman/Pearson) in den Metriken

# Skalierbares Backend für die Kernmetriken (braucht Java 17)
pyspark>=3.5

# RDF: N-Triples-Export als Input für PARIS
rdflib>=7.0

# Connectivity / Komponenten
networkx>=3.2

# Plots
matplotlib>=3.8
seaborn>=0.13

# Config / IO
pyyaml>=6.0
tqdm>=4.66

# Notebooks (optional, für explorative Auswertungen)
jupyter>=1.0
ipykernel>=6.29

# Dev
pytest>=8.0
ruff>=0.4
